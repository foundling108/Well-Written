// check if the name already exists use the read_user file
// return an empty [] or an array with 1 recorded name
// gives an error if the name has already been used
// invokes next if ok

// module.exports = (req, res, next) {
//     const dbInstance = req.app.get('db');
//     if( dbInstance.users.read_user !== [] ) {
//         alert("username is taken")
//     }
// }